\documentclass[11pt]{article}
\usepackage{parskip,times, hyperref, cite, url, graphicx, float, fancyvrb, minted}
\usepackage{tcolorbox}
\tcbuselibrary{minted,skins,breakable}
\newtcblisting{jupytercell}{
  enhanced, breakable, listing only,
  listing engine=minted,
  minted language=python,
  minted options={breaklines, fontsize=\small},
  colback=gray!6,
  colframe=blue!55!black,
  leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
  arc=2pt, left=6pt, right=4pt, top=3pt, bottom=3pt,
}
\newcommand{\jupyterinput}[1]{%
  \tcbinputlisting{
    enhanced, breakable, listing only,
    listing engine=minted,
    minted language=python,
    minted options={breaklines, fontsize=\small},
    colback=gray!6,
    colframe=blue!55!black,
    leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
    arc=2pt, left=6pt, right=4pt, top=3pt, bottom=3pt,
    listing file={#1}
  }%
}
\usepackage[margin=1.0in]{geometry}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    pdftitle={L46Project},
    pdfpagemode=FullScreen,
    }
\begin{document}

\vspace{-2em}
\centerline{\large \emph{L46 Project}}
\vspace{2em}
\centerline{\Large \emph{Exploring Quantisation In FPGA-Accelerated Binarized Neural Networks}}
\vspace{2em}
\centerline{\large Alex Chau (\emph{ac2822}), Churchill College}
\section{Introduction}
The rapid advancement of machine learning has driven an increasing demand for efficient hardware acceleration of neural networks. 
While general-purpose processors such as CPUs and GPUs have traditionally been used for this purpose, significant performance and energy efficiency gains have emerged from the use of specialised hardware accelerators.
In particular, field-programmable gate arrays (FPGAs) have gained attention due to their ability to provide flexible, fine-grained parallelism and custom dataflow architectures tailored to specific workloads.

A key technique enabling these improvements is quantisation, which reduces the numerical precision of neural network parameters and activations. 
By representing values with fewer bits, ranging from reduced fixed-point precision to fully binarized representations, quantisation significantly lowers memory usage and computational complexity. 
This, in turn, allows for faster inference, reduced power consumption, and more efficient utilisation of hardware resources. 
In extreme cases such as binarized neural networks (BNNs), arithmetic operations can be simplified to bitwise operations, making them especially well-suited for FPGA implementations.

Much of the recent progress in the AI hardware acceleration space can be attributed to the combination of increased hardware specialisation and aggressive quantisation strategies. 
Specialised architectures are able to exploit the reduced precision of quantised models to maximise parallelism and throughput, achieving performance levels that would be difficult to attain with conventional high precision designs.

This project explores the role of quantisation in FPGA-accelerated neural networks, with a particular focus on binarized neural networks.
Using the FINN framework, quantized models are mapped to dataflow-style hardware architectures on an FPGA platform. 
The study investigates how neural networks trained in Brevitas are mapped to FPGA-accelerated neural networks and how different levels of quantisation that impact performance, as well as how these effects interact with architectural parallelism. 
Through these experiments, the project aims to provide insight into the trade-offs between accuracy, resource utilisation, and performance in quantised neural network deployment on reconfigurable hardware.
\section{Experiment 1: Analysis of the PyTorch to FPGA transformation pipeline for Quantized Neural Networks}
This experiment is and end-to-end implementation of a BNN as described by \cite{}, we explore some of the black-box steps when converting a trained Brevitas/PyTorch network into an FPGA-usable bitstream.
\subsection{System Setup}
To evaluate quantized neural network acceleration on FPGA hardware, the FINN framework was deployed on a Diligent Arty-Z7-20 development board.
FINN is an open-source framework developed by Xilinx/AMD that enables deployment of quantized neural networkks (QNNs) on FPGA platforms through automated hardware generation and compilation.
\subsubsection{Hardware Platform}
The target platform used in this project is the Arty-Z7-20 development board, this board includes an Artix-7 FPGA as the programmable logic (PL) and an ARM Cortex-A9 processor in the form of a Zynq-7000 SOC as the programmable system (PL).
\subsubsection{Software Environment}
\begin{itemize}
    \item{PYNQ - A pre-built Linux image for Zynq SOCs (which interfaces with PL using Python) was flashed onto a microSD card and inserted onto the board.} 
        \begin{itemize}
            \item{There are a small number of pre-built images which AMD maintains, the Arty-Z7-20 isn't one of these boards. 
                Luckily, one of the supported boards (Pynq-Z1), happens to have the exact same specs and (almost the same) pinouts as the Arty-Z7-20, making PYNQ images built for the Pynq-Z1 compatible with the Z7-20, we use the PYNQ v3.0.1 image in this work}
        \end{itemize}
    \item{FINN - A compiler which maps Quantized Neural Networks (QNNs) trained in Brevitas to dataflow-style FPGA architectures \cite{umurogluFINNFrameworkFast2017}.}
    \item{Xilinx Design Suite 2022.2}
\end{itemize}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Images/artyBoard.png}
    \caption{Image of the Arty-Z7-20}
    \label{fig:devBoard}
\end{figure}
The rest of this subsection will detail the steps to replicate the environment for training quantized neural networks using Brevitas and converting it to a FPGA-deployable bitstream using the FINN compiler.
\subsubsection{Ubuntu 24.04}
Ubuntu 24.04 was selected as the development OS for a couple of reasons: The FINN compiler has dependencies better suited to UNIX-like operating systems, a NVIDIA GPU (RTX 3090) was the GPU available for training quantized neural networks, and Vivado tooling is hard to setup with FINN in a non-Unix environment.
Ubuntu was installed following the interactive installer, one-thing to note is that the Vivado install uses a large amount of data in the /tmp directory. Some Ubuntu install guides recommend allocating a separate 20-30GB partition purely for /tmp, we recommended allocating only a single root partition for the install so that tmp doesn't run out of memory during the Vivado Design Suite Installation, as it uses over 80GB of /tmp memory.
\subsubsection{Xilinx Design Suite 2022.2}
The FINN compiler currently only supports Vivado Design Suite 2022.2, this is slightly troublesome as the newest version of Ubuntu this Vivado version supports is Ubuntu 22.04.
Specifically, the \verb+libtinfo55+, \verb+libncurses5+ and \verb+libncurses5-dev+ libraries are missing in Ubuntu 24.04, which causes the Vivado install to hang
To install them we do the following:
\begin{verbatim}
sudo nano /etc/apt/sources.list.d/ubuntu.sources
\end{verbatim}
At the end of the file, append:
\begin{verbatim}
Types: deb
URIs: http://old-releases.ubuntu.com/ubuntu/
Suites: lunar
Components: universe
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
\end{verbatim}
Then save the file and run:
\begin{verbatim}
sudo apt update
\end{verbatim}
Then install the required libraries:
\begin{verbatim}
sudo apt install libtinfo5 libncurses5-dev libncurses5
\end{verbatim}
Now we can go to the \href{https://www.xilinx.com/support/download/index.html/content/xilinx/en/downloadNav/vivado-design-tools/archive.html}{Xilinx Download Page} to download the unified installer for version 2022.2.
\subsubsection{FINN}
The FINN compiler requires a Docker install to run without root permissions:
\begin{verbatim}
# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
\end{verbatim}
Then to install the required dependencies:
\begin{verbatim}
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
\end{verbatim}
To run docker rootless, we need to create a \verb+docker+ user group and add it to the current user.
\begin{verbatim}
sudo groupadd docker
sudo usermod -aG docker $USER
\end{verbatim}
Logging out and logging back in should update the usergroups for the current user, we can confirm that the rootless setup works by running:
\begin{verbatim}
docker run hello-world
\end{verbatim}
FINN depends on \verb+FINN_XILINX_PATH+ and \verb+FINN_XILINX_VERSION+ environment variables to point to the Xilinx tools installation directory and version.
Append the following to \verb+.bashrc+:
\begin{verbatim}
FINN_XILINX_PATH=/opt/Xilinx
FINN_XILINX_VERSION=2022.2
\end{verbatim}
We provide a fork of the FINN compiler which bundles a modified version of Brevitas:
\begin{verbatim}
git clone https://github.com/AxterDoesCode/finn.git
\end{verbatim}
We can verify that the FINN install works correctly by executing:
\begin{verbatim}
cd finn
./run-docker.sh quicktest
\end{verbatim}
To setup the GPU for use within Docker we first need the latest Nvidia \verb+cuda-drivers+ package, firstly install \verb+cuda-keyring+:
\begin{verbatim}
wget https://developer.download.nvidia.com/compute/cuda/repos/$distro/x86_64/cuda-keyring_1.1-1_all.deb
dpkg -i cuda-keyring_1.1-1_all.deb
sudo apt update
\end{verbatim}
Then install the drivers:
\begin{verbatim}
sudo apt install cuda-drivers
\end{verbatim}
Now to provide GPU access to the FINN Docker container, we need to install the Nvidia container toolkit \verb+nvidia-ctk+:
\begin{verbatim}
sudo apt-get update && sudo apt-get install -y --no-install-recommends \
   ca-certificates \
   curl \
   gnupg2
\end{verbatim}
Then add the production reposiitory:
\begin{verbatim}
curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg \
  && curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
    sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | \
    sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
\end{verbatim}
Update the packages list from the repository:
\begin{verbatim}
sudo apt-get update
\end{verbatim}
Install the container toolkit packages:
\begin{verbatim}
export NVIDIA_CONTAINER_TOOLKIT_VERSION=1.19.0-1
  sudo apt-get install -y \
      nvidia-container-toolkit=${NVIDIA_CONTAINER_TOOLKIT_VERSION} \
      nvidia-container-toolkit-base=${NVIDIA_CONTAINER_TOOLKIT_VERSION} \
      libnvidia-container-tools=${NVIDIA_CONTAINER_TOOLKIT_VERSION} \
      libnvidia-container1=${NVIDIA_CONTAINER_TOOLKIT_VERSION}
\end{verbatim}
Configure the Docker container runtime for rootless mode:
\begin{verbatim}
nvidia-ctk runtime configure --runtime=docker --config=$HOME/.config/docker/daemon.json
systemctl --user restart docker
sudo nvidia-ctk config --set nvidia-container-cli.no-cgroups --in-place
\end{verbatim}
% AlexNote: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
AlexNote TODO: Want to show that the GPU is detected in the console
\subsubsection{PYNQ}
We need to install a PYNQ image to the Zynq-7000 SoC, we use the PYNQ 3.0.1 image prebuilt for the Pynq-Z1 which can be obtained from \url{https://www.pynq.io/boards.html}.
We burn the image to a microSD card using \verb+caligula+ (which can be installed with \verb+cargo install caligula+):
\begin{verbatim}
caligula burn pynq_z1_v3.0.1.img
\end{verbatim}
The Arty-Z7-20 needs to have a couple jumpers set to allow booting from the SD card and enabling power from the micro USB port.
First we set the JP4/Boot jumper to the SD position by placing the jumper over the top two pins of JP4 as shown in \ref{fig:setupBoard1}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Images/SetupBoard1.png}
    \caption{Image of the Arty-Z7-20 JP4 Jumper}
    \label{fig:setupBoard1}
\end{figure}
Then to power the Arty-Z7-20 from a micro USB cable, set the JP5/Power jumper to the USB position by placing the jumper over the two left pins as shown in \ref{fig:setupBoard2}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Images/SetupBoard2.png}
    \caption{Image of the Arty-Z7-20 JP5 Jumper}
    \label{fig:setupBoard2}
\end{figure}
Now we can insert the microSD card we flashed with PYNQ, connect the board via ethernet to the Ubuntu device, and power it via micro USB,
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=1\linewidth]{Images/SetupBoard2.jpeg}
%     \caption{Arty-Z7-20 with successfully flashed PYNQ image}
%     \label{fig:setupBoard3}
% \end{figure}
We can identify the device connected to the Ubuntu pc successfully by executing:
\begin{verbatim}
ip neigh
\end{verbatim}
Which outputs something like:
\begin{verbatim}
192.168.2.247 dev enxd0c0bf48c3bb lladdr 00:17:3e:e3:a1:63 REACHABLE
\end{verbatim}
This \verb+enxd0c0bf48c3bb+ is the ethernet interface that the development board is connected to.

We add a ssh key to the board, replacing PATH\_TO\_YOUR\_PUBKEY with a public key created by \verb+ssh-keygen+
The default user on Pynq images is \verb+xilinx+ with the root password also being \verb+xilinx+.
\begin{verbatim}
ssh-copy-id -i PATH_TO_YOUR_PUBKEY xilinx@192.168.2.247
\end{verbatim}
The Zynq-7000 needs internet access, as it connects directly to the Ubuntu pc we can setup a network bridge so that the board gets internet access.
Locate the wired network tab in Ubuntu settings:
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Images/SetupNetwork1.png}
    \caption{Ubuntu wired network tab}
    \label{fig:setupNetwork1}
\end{figure}
Clicking the cog, we access the IPv4 tab where we set the IPv4 method to \verb+Shared to other computers+
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Images/SetupNetwork2.png}
    \caption{Ethernet interface IPv4 forwarding}
    \label{fig:setupNetwork2}
\end{figure}
Now we can ssh into the PS of the board:
\begin{verbatim}
ssh xilinx@192.168.2.247
\end{verbatim}
And we can verify that the board now has internet access:
\begin{verbatim}
ping google.com
\end{verbatim}
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Images/confirmNetwork.png}
    \caption{Terminal output from network test}
    \label{fig:confirmNetwork}
\end{figure}
\subsection{Training a BNN using Brevitas}
Training a Binarized Neural Network (BNN) using the Brevitas library follows a very similar workflow to training a conventional Convolutional Neural Network (CNN) in PyTorch. The overall structure of defining a model, specifying a loss function, and performing optimisation through backpropagation remains unchanged. 

The key difference lies in the use of quantised layers provided by Brevitas. Instead of standard PyTorch layers, such as \texttt{Conv2d} or \texttt{Linear}, Brevitas introduces quantised counterparts (e.g. \texttt{QuantConv2d}, \texttt{QuantLinear}, and \texttt{QuantIdentity}) that simulate reduced numerical precision during training. 
These layers allow both weights and activations to be constrained to a fixed bit-width, enabling the network to approximate the behaviour of low-precision hardware implementations.

In addition, quantisation-aware training requires careful handling of value ranges and scaling factors. For example, inputs are typically normalised to a fixed range (e.g. $[-1, 1]$), and intermediate activations are passed through explicit quantisation layers. 
Batch normalisation is essential after each layer to stabilise training, as quantized networks are generally more sensitive to changes in data distribution.

The model architecture consists of a sequence of quantised convolutional layers for feature extraction, followed by fully connected layers for classification. 
The full implementation of the model is shown below, the training code and transformation ordering is largely based on the Xilinx integration test suite, which AMD recommends to use as a template.
\jupyterinput{./Code/model.py}
The train script is split into two parts: \texttt{trainer.py} which exposes a trainer class, and \texttt{train.py} which parses in arguments and is called to actually train a model.
\jupyterinput{./Code/trainer.py}
\jupyterinput{./Code/train.py}
The relative utilities used in the model and trainscripts are:
\jupyterinput{./Code/common.py}
\jupyterinput{./Code/logger.py}
\jupyterinput{./Code/losses.py}
\jupyterinput{./Code/tensorNorm.py}
To train a binarized neural network we then need a configuration file which specifies the parameters of the QNN:
\begin{verbatim}
# CNV_1W1A.ini
[MODEL]
ARCH: CNV
DATASET: CIFAR10
IN_CHANNELS: 3
NUM_CLASSES: 10

[QUANT]
WEIGHT_BIT_WIDTH: 1
ACT_BIT_WIDTH: 1
IN_BIT_WIDTH: 8
\end{verbatim}
Inside the FINN install directory we enter the FINN Docker environment using:
\begin{verbatim}
cd finn
./run-docker.sh
\end{verbatim}
Then we train a binarized neural network with the following command:
\begin{verbatim}
BREVITAS_JIT=1 python train.py --network CNV_1W1A --experiments ./experiments --epochs 500 --batch_size 512 --gpus 0
\end{verbatim}
AlexNote TODO: add the GPU training results log here
AlexNote TODO: add the GPU training results log here
AlexNote TODO: add the GPU training results log here
AlexNote TODO: add the GPU training results log here
AlexNote TODO: add the GPU training results log here
AlexNote TODO: add the GPU training results log here

Now with a trained model, we can export the model to QONNX by executing:
\begin{verbatim}
BREVITAS_JIT=0 python train.py --evaluate --network CNV_1W1A --resume ./experiments/path/to/checkpoint.tar --export_qonnx --gpus 0 --batch_size 512
\end{verbatim}
\subsection{Preparing a trained Brevitas network for hardware synthesis}
We open a Jupyter notebook instance inside the FINN directory with:
\begin{verbatim}
./run-docker.sh notebook
\end{verbatim}
(The transformation code was run inside a Jupyter notebook and will be formatted in a cell-like manner)

First some basic imports and config:
\begin{jupytercell}
from finn.util.basic import make_build_dir
from finn.util.visualization import showInNetron
import os

build_dir = os.environ["FINN_BUILD_DIR"]
notebook_dir = os.environ['FINN_ROOT'] + "/notebooks"
model_name = "cnv_w1a1"
\end{jupytercell}

\begin{jupytercell}
import torch
import onnx
from brevitas.export import export_qonnx
from qonnx.util.cleanup import cleanup as qonnx_cleanup
from qonnx.core.modelwrapper import ModelWrapper
from finn.transformation.qonnx.convert_qonnx_to_finn import ConvertQONNXtoFINN
from qonnx.transformation.infer_shapes import InferShapes
from qonnx.transformation.fold_constants import FoldConstants
from qonnx.transformation.general import GiveReadableTensorNames, GiveUniqueNodeNames, RemoveStaticGraphInputs
from qonnx.transformation.infer_datatypes import InferDataTypes

print(build_dir)
export_onnx_path = notebook_dir + "/L46Stuff" + f"/{model_name}_export.onnx"
showInNetron(export_onnx_path)
\end{jupytercell}
With this we can visualise the QONNX graph generated from training:
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/InitialQONNX_p1.png}
    \caption{Initial ONNX graph before transformations (1/4)}
    \label{fig:InitialGraph}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/InitialQONNX_p2.png}
    \caption{Initial ONNX graph before transformations (2/4)}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/InitialQONNX_p3.png}
    \caption{Initial ONNX graph before transformations (3/4)}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/InitialQONNX_p4.png}
    \caption{Initial ONNX graph before transformations (4/4)}
\end{figure}
Next we convert the model from QONNX to FINN-ONNX:
\begin{jupytercell}
qonnx_cleanup(export_onnx_path, out_file=export_onnx_path)
model = ModelWrapper(export_onnx_path)
model = model.transform(ConvertQONNXtoFINN())
model.save(build_dir + f"/end2end_{model_name}_FINNONNX.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_FINNONNX.onnx")
\end{jupytercell}
AlexNote TODO: Add the ConvertQONNXtoFINN netron graph here and discuss what it does.

We perform some basic transformations to prepare the graph for upcoming transformations, these include:
\begin{itemize}
    \item \verb+GiveUniqueNodeNames+
    \item \verb+GiveReadableTensorNames+
    \item \verb+InferShapes+
    \item \verb+InferDataTypes+
    \item \verb+FoldConstants+
    \item \verb+RemoveStaticGraphInputs+
\end{itemize}
The transformations \verb+GiveUniqueNodeNames+ and \verb+GiveReadableTensorNames+ give the nodes in the graph unique names by their enumeration, tensors are then given human-readable names based on the node names.
The \verb+InferShapes+ and \verb+InferDataTypes+ transformations derive shapes and data types of the tensors from the model properties and sets them in the \verb+ValueInfo+ property of the model.
These four transformations do not affect the structure of the graph.

The remaining two transformations modify the structure of the graph, \verb+FoldConstants+ identifies nodes with constant inputs and calculates the output, then for the following node the inputs are set as constant-only inputs and the previous node is deleted.
\verb+FoldConstants+ is useful for FPGA synthesis as it reduces the number of nodes (blocks) that need to be stitched together in the final design.
The other mutating basic transformation is \verb+RemoveStaticGraphInputs+, this transformation removes any top-level graph inputs that already are associated with ONNX initialisers.

Executing this cell transforms the model into Fig~\ref{fig:AfterTidyUp}
\begin{jupytercell}
model = model.transform(InferShapes())
model = model.transform(FoldConstants())
model = model.transform(GiveUniqueNodeNames())
model = model.transform(GiveReadableTensorNames())
model = model.transform(InferDataTypes())
model = model.transform(RemoveStaticGraphInputs())
model.save(build_dir + f"/end2end_{model_name}_tidy.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_tidy.onnx")
\end{jupytercell}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/AfterTidyUp_p1.png}
    \caption{FINN-ONNX graph after tidy-up transformations (1/4)}
    \label{fig:AfterTidyUp}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/AfterTidyUp_p2.png}
    \caption{FINN-ONNX graph after tidy-up transformations (2/4)}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/AfterTidyUp_p3.png}
    \caption{FINN-ONNX graph after tidy-up transformations (3/4)}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.88\textheight, keepaspectratio]{Images/Experiment1Graphs/split/AfterTidyUp_p4.png}
    \caption{FINN-ONNX graph after tidy-up transformations (4/4)}
\end{figure}
We can see that the CNN has been transformed into a sequential network as a result of the mutating changes removing constants and static graph inputs.
For example, the first \verb+BipolarQuant+ node in Fig~\ref{fig:InitialGraph} has been moved into the \verb+conv+ node in \ref{fig:AfterTidyUp}.
From this netron representation, it is clear now that the model consists of an initial \verb+MultiThreshold+ layer which quantizes float input into 8-bit, followed by repeating convolution-convolution-maxpool layer pattern with 3x3 filters for feature extraction. Finally followed by fully-connected layers for classification.
Transforming the network into a sequential set of nodes is useful for FPGA deployment as it reduces the amount of hardware resources used to implement the network.
Additionally it allows for easier implementation of selective parallelism, which is explored more in \ref{Experiment3}.

Next we add some preprocessing and postprocessing steps to the model.
For preprocessing we want to divide the 8-bit input data by 255 so that the inputs to the network are bounded between 0 and 1.
This is achieved by creating a QONNX node using the \verb+ToTensor()+ function, converting it to FINN-ONNX and merging the models together.

For postprocessing we want to take the output of the network and return the index of of the image category with the highest probability, this should return an index from 0-9 as the model is based on CIFAR-10.
This node can be directly inserted at the end of the model via a transformation with \verb+InsertTopK()+ without having to do the intermediate representation transformations as performed in pre-processing.
\begin{jupytercell}
from finn.util.pytorch import ToTensor
from qonnx.transformation.merge_onnx_models import MergeONNXModels
from qonnx.core.datatype import DataType
from qonnx.transformation.insert_topk import InsertTopK
from qonnx.transformation.infer_datatypes import InferDataTypes

model = ModelWrapper(build_dir+f"/end2end_{model_name}_tidy.onnx")
global_inp_name = model.graph.input[0].name
ishape = model.get_tensor_shape(global_inp_name)
# preprocessing: torchvision's ToTensor divides uint8 inputs by 255
totensor_pyt = ToTensor()
chkpt_preproc_name = build_dir+f"/end2end_{model_name}_preproc.onnx"
export_qonnx(totensor_pyt, torch.randn(ishape), chkpt_preproc_name)
qonnx_cleanup(chkpt_preproc_name, out_file=chkpt_preproc_name)
pre_model = ModelWrapper(chkpt_preproc_name)
pre_model = pre_model.transform(ConvertQONNXtoFINN())

# join preprocessing and core model
model = model.transform(MergeONNXModels(pre_model))
# add input quantization annotation: UINT8 for all BNN-PYNQ models
global_inp_name = model.graph.input[0].name
model.set_tensor_datatype(global_inp_name, DataType["UINT8"])

# postprocessing: insert Top-1 node at the end
model = model.transform(InsertTopK(k=1))
chkpt_name = build_dir+f"/end2end_{model_name}_pre_post.onnx"
# tidy-up again
model = model.transform(InferShapes())
model = model.transform(FoldConstants())
model = model.transform(GiveUniqueNodeNames())
model = model.transform(GiveReadableTensorNames())
model = model.transform(InferDataTypes())
model = model.transform(RemoveStaticGraphInputs())
model.save(chkpt_name)
\end{jupytercell}
\begin{jupytercell}
showInNetron(build_dir+f"/end2end_{model_name}_pre_post.onnx")
\end{jupytercell}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.2\textheight, keepaspectratio]{Images/Experiment1Graphs/AfterPreProcessingStartNode.png}
    \caption{FINN-ONNX graph preprocessing node}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.2\textheight, keepaspectratio]{Images/Experiment1Graphs/AfterPostProcessing.png}
    \caption{FINN-ONNX graph postprocessing node}
\end{figure}
Looking at the start of the graph, a \verb+Div+ node that we merged in has been added for input preprocessing.
At the tail of the graph you can observe a \verb+TopK+ node which returns the index for the most likely output.

Now the network needs to be processed further for hardware synthesis, we target the convolutional layers next.
Convolutions in FINN are implemented with convolution lowering, this approach converts convolutional layers into matrix mutiply opeerations, where one of the matrices is generated by a sliding window from the input image.
Two primitives in the FINN RTL library are the Sliding window unit (SWU) and Matrix Vector Activation Unit (MVAU), the following operations transform the network into ONNX equivalents of these hardware primitives.

First we perform some small transformations and \verb+streamline+ the network:
\begin{jupytercell}
from finn.transformation.streamline import Streamline
from qonnx.transformation.lower_convs_to_matmul import LowerConvsToMatMul
from qonnx.transformation.bipolar_to_xnor import ConvertBipolarMatMulToXnorPopcount
import finn.transformation.streamline.absorb as absorb
from finn.transformation.streamline.reorder import MakeMaxPoolNHWC, MoveScalarLinearPastInvariants
from qonnx.transformation.infer_data_layouts import InferDataLayouts
from qonnx.transformation.general import RemoveUnusedTensors

model = ModelWrapper(build_dir + f"/end2end_{model_name}_pre_post.onnx")
model = model.transform(absorb.AbsorbSignBiasIntoMultiThreshold())
model = model.transform(MoveScalarLinearPastInvariants())
model = model.transform(Streamline())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}

Now we can perform the lowering transformation on the convolution layers
\begin{jupytercell}
model = model.transform(LowerConvsToMatMul())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}

Next we apply the transformations \verb+MakeMaxPoolNHWC+ and \verb+AbsorbTransposeIntoMultiThreshold+, these transformations converts the data layout of the FINN-ONNX nodes back into ONNX-like data layouts.
Specifically, the tensor dimensions are reordered as NCHW where (N: Batch,  H: Height, W: Width, C: Channels). The reason for doing this is because the underlying RTL libraries which FINN maps the model to are also structured as NHWC.

\begin{jupytercell}
model = model.transform(MakeMaxPoolNHWC())
model = model.transform(absorb.AbsorbTransposeIntoMultiThreshold())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}

\begin{jupytercell}
model = model.transform(ConvertBipolarMatMulToXnorPopcount())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}

\begin{jupytercell}
model = model.transform(Streamline())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
showInNetron(build_dir+f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}

\begin{jupytercell}
model = model.transform(absorb.AbsorbScalarMulAddIntoTopK())
model = model.transform(InferDataLayouts())
model = model.transform(RemoveUnusedTensors())
model.save(build_dir + f"/end2end_{model_name}_streamlined.onnx")
\end{jupytercell}
\subsection{Evaluating the BNN compared to running on a GPU // Something like this}
\section{Experiment 2: Impact of quantisation on neural network FPGA performance}
\section{Experiment 3: Investigating parallelism in FPGA QNN performance}\label{Experiment3}
Basically see if parallelism with certain quantisation sizes has some impact.
Use minimal, small, and the w1a1 things in hardware
\newpage
\bibliography{library}{}
\bibliographystyle{plainurl}
\end{document}
